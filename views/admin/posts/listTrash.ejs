<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <% include ../components/head.ejs %>
    <link rel="stylesheet" href="/admin/css/list.css">
    <title>Posts</title>
</head>
<body>
<% include ../components/sidebar.ejs %>
<div class="main">
    <h1 class="st-h1">Posts Trash</h1>
    <div class="table-wrapper">
        <table>
            <thead>
            <tr>
                <th class="id_header">id</th>
                <th class="title_header">Title</th>
                <th class="tag_header">Tags</th>
                <th class="date_header">Date</th>
            </tr>
            </thead>
            <tbody>
            <% posts.forEach(post => { %>
                <tr class="item">
                    <td><%= post.id %></td>
                    <td class="item-title <%= !post.display ? "off" : "" %>"><a
                                href="/admin/posts/<%= post.id %>/recover?_csrf=<%= csrfToken %>"
                                class="recover"><%= post.getTextTitle() %></a></td>
                    <td class="tags">
                        <% post.tags.slice(0, 5).forEach(tag => { %>
                            <span class="tag"><%= tag %></span>
                        <% }); %>
                    </td>
                    <td class="date"><%= post.date.toLocaleString() %></td>
                    <td><a href="/admin/posts/<%= post.id %>/delete?_csrf=<%= csrfToken %>"><span
                                    class="button x-button">✕</span></a></td>
                </tr>
            <% }); %>
            </tbody>
        </table>
    </div>
</div>
<script>
    document.querySelector('.x-button').addEventListener("click", evt => {
        if (!confirm("Are you sure you would like to delete this post? This action CANNOT be undone.")) {
            evt.preventDefault();
        }
    });
    document.querySelector('.recover').addEventListener("click", evt => {
        if (!confirm("Are you sure you would like to recover this post?")) {
            evt.preventDefault();
        }
    });
</script>
</body>
</html>
